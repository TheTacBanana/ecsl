fn main() {
    assert_true(1 + 1 == 2);
    assert_true(1 - 1 == 0);
    assert_true(1 * 2 == 2);
    assert_true(1 / 1 == 1);

    assert_true(1.0 + 1.0 == 2.0);
    assert_true(1.0 - 1.0 == 0.0);
    assert_true(1.0 * 2.0 == 2.0);
    assert_true(1.0 / 1.0 == 1.0);
}
