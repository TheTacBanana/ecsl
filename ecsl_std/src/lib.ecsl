[prelude]
use {
    std::num::{int, float},
    std::bool::bool,
    std::char::char,
    std::string::str,
    std::lib::drop,
    std::fmt::{print, print_int, print_float, print_bool},
    std::panic::{panic, assert_true, assert_false},
    std::option::Option,
};

//fn drop<T>(val: T) { }
