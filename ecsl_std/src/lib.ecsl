[prelude]
use {
    std::num::{int, float},
    std::bool::bool,
    std::char::char,
    std::lib::drop,
    std::fmt::{print_int, print_float, print_bool},
    std::panic::panic,
    std::option::Option,
};

//fn drop<T>(val: T) { }
