use bool::bool;

[terminator]
fn panic() {
    byt {
        PANIC;
    }
}

fn assert_true(cond: bool) {
    if (cond) {
        return;
    }
    panic();
}

fn assert_false(cond: bool) {
    if (!cond) {
        return;
    }
    panic();
}
